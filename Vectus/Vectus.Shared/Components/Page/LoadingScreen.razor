<div class="animated-loader-overlay">
	<div class="animated-loader-container">
		<img src="_content/Vectus.Shared/images/loading.svg" alt="Loading" class="animated-loader-image" />
		<p class="animated-loader-text">Checking the Tyre Pressure on the Servers.</p>
	</div>
</div>

<style>
	.animated-loader-overlay {
		position: fixed;
		top: 0;
		left: 0;
		right: 0;
		bottom: 0;
		background: var(--app-bg);
		display: flex;
		align-items: center;
		justify-content: center;
		z-index: 9999;
	}

	.animated-loader-container {
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 24px;
	}

	.animated-loader-image {
		width: 400px;
		height: 400px;
	}

	.animated-loader-text {
		margin: 0;
		color: var(--gray-900);
		font-size: 18px;
		font-weight: 600;
		letter-spacing: 0.2px;
		text-align: center;
	}

	/* Responsive */
	@@media (max-width: 576px) {
		.animated-loader-image {
			width: 200px;
			height: 200px;
		}

		.animated-loader-text {
			font-size: 15px;
			padding: 0 12px;
		}
	}
</style>
